services:
  web:
    image: nginx
    ports:
      - "127.0.0.1:8080:80"
    restart: unless-stopped

  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: testpass
    restart: unless-stopped
